---
all:
  vars:
    ansible_host: localhost
    ansible_connection: local
    head: head
  children:
    cluster:
      children:
        head:
          hosts: localhost
        work:
          hosts: localhost
      vars:
        cluster_node_ip: 10.0.0.1
        cluster_node_prefixlen: 24
    host:
      hosts: localhost
    guest:
      hosts: localhost
      vars:
        hv_node_ip: 1.1.1.1
        hv_node_netmask: 255.255.255.0
        hv_node_broadcast: 1.1.1.255
bootstrap:
  children: cluster
